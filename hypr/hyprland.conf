# All keys: https://github.com/xkbcommon/libxkbcommon/blob/master/include/xkbcommon/xkbcommon-keysyms.h
#
# Please note not all available settings / options are set here.
# For a full list, see the wiki (basic and advanced configuring)
#

# monitor=,preferred,1920x0,1

general {
    sensitivity=1.0 # for mouse cursor
    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)
    main_mod=SUPER
    border_size=2
    gaps_in=5
    gaps_out=10
    col.active_border=0x000000 #0x66ee1111
    col.inactive_border=0x000000 #0x66333333
    damage_tracking=full # leave it on full unless you hate your GPU and want to make it suffer
}

decoration {
    rounding=15
    blur_size=3 # minimum 1
    blur_passes=1 # minimum 1, more passes = more resource intensive.
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
    blur_new_optimizations=1
    drop_shadow=1
    shadow_range=15
    shadow_render_power=2
}

animations {
    enabled=1
    animation=windows,1,7,default
    animation=border,1,10,default
    animation=fade,1,10,default
    animation=workspaces,1,6,default
}

input {
    kb_layout=it
    follow_mouse=1

    touchpad {
        natural_scroll=yes
    }
}

gestures {
    workspace_swipe=yes
    workspace_swipe_fingers=3
}

misc {
	disable_hyprland_logo = yes
	mouse_move_enables_dpms = yes
}

dwindle {
    pseudotile=1 # enable pseudotiling on dwindle
    force_split=0 # 0 means the split direction follows mouse position relative to the window
    preserve_split=1
}



# for windows named/classed as abc and xyz
windowrule=float,flameshot
windowrule=fullscreen,flameshot

# example binds
# bind=SUPER,Q,exec,kitty
bind=SUPERSHIFT,P,exec,wofi-power.sh # https://raw.githubusercontent.com/luispabon/sway-dotfiles/master/scripts/wofi-power.sh
bind=SUPERSHIFT,M,exit
bind=SUPERSHIFT,A,exec,nwg-drawer
bind=SUPERSHIFT,W,exec,wofi-wifi.sh

bind=SUPER,Q,killactive
bind=SUPER,T,exec,kitty 
bind=SUPER,E,exec,nautilus # dolphin
bind=SUPER,V,togglefloating,
bind=SUPER,A,exec,sirula
bind=SUPER,F,fullscreen
#bind=SUPER,D,exec,wofi --show drun # -o DP-3

bind=SUPER,left,movefocus,l
bind=SUPER,right,movefocus,r
bind=SUPER,up,movefocus,u
bind=SUPER,down,movefocus,d

bind=SUPERSHIFT,left,movewindow,l
bind=SUPERSHIFT,right,movewindow,r
bind=SUPERSHIFT,up,movewindow,u
bind=SUPERSHIFT,down,movewindow,d

bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9
bind=SUPER,0,workspace,10

bind=SUPERSHIFT,1,movetoworkspace,1
bind=SUPERSHIFT,2,movetoworkspace,2
bind=SUPERSHIFT,3,movetoworkspace,3
bind=SUPERSHIFT,4,movetoworkspace,4
bind=SUPERSHIFT,5,movetoworkspace,5
bind=SUPERSHIFT,6,movetoworkspace,6
bind=SUPERSHIFT,7,movetoworkspace,7
bind=SUPERSHIFT,8,movetoworkspace,8
bind=SUPERSHIFT,9,movetoworkspace,9
bind=SUPERSHIFT,0,movetoworkspace,10

bind = ALT,1,movetoworkspacesilent,1
bind = ALT,2,movetoworkspacesilent,2
bind = ALT,3,movetoworkspacesilent,3
bind = ALT,4,movetoworkspacesilent,4
bind = ALT,5,movetoworkspacesilent,5
bind = ALT,6,movetoworkspacesilent,6
bind = ALT,7,movetoworkspacesilent,7
bind = ALT,8,movetoworkspacesilent,8
bind = ALT,9,movetoworkspacesilent,9
bind = ALT,0,movetoworkspacesilent,10

bind=SUPER,mouse_down,workspace,e+1
bind=SUPER,mouse_up,workspace,e-1

# Brightness
bind=,XF86MonBrightnessUp,exec,brightnessctl set 5%+
bind=,XF86MonBrightnessDown,exec,brightnessctl set 5%-

# Speaker volume
bind=,XF86AudioRaiseVolume,exec,pamixer -i 5
bind=,XF86AudioLowerVolume,exec,pamixer -d 5
bind=,XF86AudioMute,exec,pamixer --toggle-mute

# Mic volume
bind=SUPER,XF86AudioRaiseVolume,exec,pamixer --default-source -i 5
bind=SUPER,XF86AudioLowerVolume,exec,pamixer --default-source -d 5
bind=,XF86AudioMicMute,exec,pamixer --default-source --toggle-mute

# Lock scren
bind=SUPER,L,exec,systemctl --user start lock

# Screenshot
bind=,XF86SelectiveScreenshot,exec,XDG_CURRENT_DESKTOP=sway flameshot gui
bind=,Print,exec,XDG_CURRENT_DESKTOP=sway flameshot gui

# allow for screen recording via xdg-desktop-portal-wlr
# Official from guide
# exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
# From morro .dotfiles
exec-once=systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK
exec-once=hash dbus-update-activation-environment 2>/dev/null && dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP

# lock suspend deamon
exec-once=/home/js/bin/lock_suspend.sh

# notification daemon
exec-once=dunst

# status bar
exec-once=waybar

# background
exec-once=swaybg -i /home/js/gitClone/WhiteSur-wallpapers/Wallpaper-nord/Monterey-nord.png

# disk automount
exec-once=udiskie -an --no-tray -f nautilus	# udiskie and udisks2 must be installed. Change nautilus with whatever you like

# startup applications
exec-once=syncthing serve redshift

exec-once=systemctl --user start xdg-desktop-portal-gtk
exec-once=systemctl --user start xdg-desktop-portal
exec-once=systemctl --user start xdg-desktop-portal-wlri

#Startup Apps
exec-once=startupApps.sh
